import { NumTransformPipe } from './num-transform.pipe';

describe('NumTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new NumTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
